import { CSSProperties, PropsWithChildren, useEffect, useState } from "react";

export const OrientationProvider = (props: PropsWithChildren) => {
    return (
        <>
            {props.children}
        </>
    )
}