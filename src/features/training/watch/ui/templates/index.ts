import { BlockSounds } from "./BlockSounds";
import { BlockVideos } from "./BlockVideos";
import { BlockQty } from "./BlockQty";
import { BlockText }   from "@/features/training/watch/ui/templates/BlockText";

export const WatchTrainingTemplate = {
    BlockSounds,
    BlockVideos,
    BlockText,
    BlockQty
}